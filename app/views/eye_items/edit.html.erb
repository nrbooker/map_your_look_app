<!-- Validation failure messages -->
<% if @eye_item.errors.any? %>
  <% @eye_item.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Edit Eye Item #<%= @eye_item.id %></h3>
</div>

<div class="row">
  <div class="col-md-12">
    <%=form_for (@eye_item), :html => {:multipart => true} do |f|%>
    <%= f.label :store, "Makeup Type" %><br>
    <%= f.text_field :store, class: 'form-control'%><br>
    <%= f.label :brand, "Brand" %><br>
    <%= f.text_field :brand, class: 'form-control'%><br>
    <%= f.label :color, "Color" %><br>
    <%= f.text_field :color, class: 'form-control'%><br>
    <%= f.submit "Update Item", class: "btn btn-success" %>
or
      <a href="/looks">Cancel</a>

  <% end %>
  </div>
</div>
